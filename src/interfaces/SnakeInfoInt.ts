/**
 * Expected structure of the response to send
 * for the root `/` endpoint.
 */
export interface SnakeInfoInt {
  apiversion: string;
  author: string;
  color: string;
  head: string;
  tail: string;
}
